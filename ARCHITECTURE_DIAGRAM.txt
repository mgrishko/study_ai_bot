╔═══════════════════════════════════════════════════════════════════════════╗
║             PAYMENT INTEGRATION - ARCHITECTURE DIAGRAM                    ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│                          BOT ENTRY POINT                                │
│                            bot.py                                        │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ • Initializes PostgreSQL database                                  │ │
│  │ • Sets up aiohttp webhook server (port 8080)                       │ │
│  │ • Registers routers (common, payment, user, admin, ai)             │ │
│  │ • Validates LiqPay configuration                                   │ │
│  │ • Starts polling for Telegram messages                             │ │
│  └────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
           ┌────────▼────────┐ ┌────▼─────────┐ ┌──▼──────────────┐
           │   DB Connection │ │   Handlers   │ │  Webhook Server │
           │  (asyncpg pool) │ │   (routers)  │ │  (aiohttp app)  │
           └─────────────────┘ └──────────────┘ └─────────────────┘

═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│                      ORDER CREATION FLOW                                │
└─────────────────────────────────────────────────────────────────────────┘

User in Telegram                    Bot Handlers                Database
    │                                  │                           │
    ├─ Click "Каталог"                │                           │
    │                                  │                           │
    ├─ Select Product                  │                           │
    │                                  │                           │
    ├─ Click "Купити"                  │                           │
    │                    ┌─ command_buy_handler()                  │
    │                    │  • Store product_id, quantity           │
    │                    │  • Transition to OrderStates.             │
    │                    │    waiting_for_contact_info             │
    │                    │  • Show contact form                    │
    │                    │                                          │
    ├─ Fill name, phone, email        │                           │
    │                                  │                           │
    ├─ Confirm with "Так"              │                           │
    │                    ┌─ confirm_order_with_contact()           │
    │                    │  • Validate input                       │
    │                    │  • Create order in DB ────────────────┐ │
    │                    │  • Store order_id in FSM              │ │
    │                    │  • Transition to                       └─┼──┐
    │                    │    PaymentStates.                      │ │  │
    │                    │    waiting_for_payment_method           │ │  │
    │                    │  • Show payment button                 │ │  │
    │                    │                                          │ │  │

═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│                    PAYMENT FLOW - HANDLER CHAIN                         │
└─────────────────────────────────────────────────────────────────────────┘

User in Telegram            Bot Handlers                     Services
    │                            │                              │
    ├─ Click "💳 Оплатити"       │                              │
    │   замовлення"              │                              │
    │                  ┌── proceed_to_payment()                 │
    │                  │ • Get order_id from FSM                │
    │                  │ • Get order details from DB ───────────┤ get_order()
    │                  │ • Show payment method keyboard          │
    │                  │                                        │
    ├─ Click "💳 LiqPay"        │                              │
    │                  ┌──select_payment_method()               │
    │                  │ • Get order_id from FSM                │
    │                  │ • Verify order exists ────────────────┤ get_order()
    │                  │ • Store method in FSM                  │
    │                  │ • Call handle_liqpay_payment()         │
    │                  │                                        │
    │                  ├──handle_liqpay_payment()               │
    │                  │ • Build payment data ────────────────┐ │
    │                  │   (amount, currency, order_id, etc)  │ │
    │                  │ • Call LiqPayService.                │ │
    │                  │   generate_payment_url() ─────────────┤─── LiqPayService
    │                  │                                        │  │ • Encode data
    │                  │ • Create payment record ──────────────┤─── Database
    │                  │   in DB                               │
    │                  │ • Show payment link button             │
    │                  │                                        │
    ├─ Click payment link       │                              │
    │ (URL)                     │                              │
    │                           │  ◄─ Redirect to LiqPay      │
    │
    │─────────────────────────────────► LiqPay Checkout
    │                                     (User enters card)
    │
    │◄────────────────────────────────── LiqPay Callback
    │                                   (Webhook POST)

═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│                  WEBHOOK PROCESSING - CALLBACK FLOW                     │
└─────────────────────────────────────────────────────────────────────────┘

LiqPay Server              Webhook Server              Bot Services
    │                            │                           │
    ├─ POST /webhook/liqpay      │                           │
    │ ├─ data: base64 JSON       │                           │
    │ ├─ signature: SHA1 hash     │                           │
    │                  ┌─────────────────────────────────────┤
    │                  │ handle_liqpay_webhook()             │
    │                  │ • Get data and signature            │
    │                  │ • Decode base64 data                │
    │                  │ • Verify signature ────────────────┤─── LiqPayService
    │                  │   (constant-time compare)           │ • verify_callback()
    │                  │ • Get order_id from data            │
    │                  │ • Get payment status from LiqPay    │
    │                  │                                    │
    │                  ├─ IF signature valid:               │
    │                  │                                    │
    │                  │ • Get payment record ─────────────┤─── Database
    │                  │   (by order_id)                    │  │ get_payment_
    │                  │                                    │     by_order()
    │                  │ • Update payment.status ──────────┤─── Database
    │                  │   (success/failure)                 │  │ update_
    │                  │                                    │     payment_
    │                  │ • Get order details ──────────────┤─── Database
    │                  │                                    │  │ get_order()
    │                  │                                    │
    │                  ├─ IF status == "success":          │
    │                  │                                    │
    │                  │ • Update order.payment_status ───┤─── Database
    │                  │   to "paid"                        │  │ update_
    │                  │                                    │     order_
    │                  │ • Store payment transaction ID      │     payment_
    │                  │                                    │     info()
    │                  │ • Log success                       │
    │                  │ • Send 200 OK response              │
    │                  │                                    │
    │                  ├─ ELSE (invalid signature):        │
    │                  │ • Log warning                       │
    │                  │ • Send 401 Unauthorized             │
    │                  │                                    │
    │◄────────────────┤ Return 200 OK / 401 Unauthorized   │
    │                  │                                    │

═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│                         DATABASE SCHEMA                                 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐         ┌──────────────────────┐
│      orders         │         │     payments         │
├─────────────────────┤         ├──────────────────────┤
│ id (PK)             │◄────────│ order_id (FK, unique)│
│ user_id (FK)        │         │ id (PK)              │
│ product_id (FK)     │         │ user_id (FK)         │
│ quantity            │         │ amount               │
│ phone               │         │ currency             │
│ email               │         │ payment_method       │
│ total_price         │         │ status               │
│ status              │         │ liqpay_payment_id    │
│ created_at          │         │ liqpay_order_id      │
│ updated_at          │         │ telegram_payment_id  │
│ payment_status ◄────┼─────────│ error_message        │
│ payment_method ◄────┼─────────│ created_at           │
│                     │         │ updated_at           │
└─────────────────────┘         └──────────────────────┘

payment_status values:
  • unpaid (default)
  • paid (after successful webhook)
  • failed (after failed webhook)

═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│                    FSM STATE TRANSITIONS                                │
└─────────────────────────────────────────────────────────────────────────┘

ORDER STATES (handlers/order_states.py):
  ┌────────────────────────────────────────────────────────────────────┐
  │ waiting_for_product_selection                                      │
  │         │                                                          │
  │         ▼                                                          │
  │ waiting_for_quantity                                              │
  │         │                                                          │
  │         ▼                                                          │
  │ waiting_for_contact_info                                          │
  │         │                                                          │
  │         ▼                                                          │
  │ waiting_for_confirmation                                          │
  │         │                                                          │
  │         ▼                                                          │
  │ TRANSITION TO PAYMENT STATES ──┐                                  │
  └────────────────────────────────┼─────────────────────────────────┘
                                    │
PAYMENT STATES (handlers/payment_states.py):
  ┌────────────────────────────────┼─────────────────────────────────┐
  │                                 │                                │
  │                    ┌────────────▼─────────────┐                 │
  │                    │ waiting_for_payment_     │                 │
  │                    │ method                   │                 │
  │                    └────────────┬─────────────┘                 │
  │                                 │                                │
  │                 ┌───────────────┼───────────────┐               │
  │                 │               │               │               │
  │    ┌────────────▼──────────┐    │    ┌─────────▼──────────┐    │
  │    │ waiting_for_liqpay_   │    │    │ waiting_for_       │    │
  │    │ confirmation          │    │    │ telegram_payment   │    │
  │    └────────────┬──────────┘    │    └─────────┬──────────┘    │
  │                 │               │              │                │
  │                 └───────────────┼──────────────┘                │
  │                                 │                                │
  │                    ┌────────────▼─────────────┐                 │
  │                    │ payment_completed       │                 │
  │                    └─────────────────────────┘                 │
  └──────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│                   FILE DEPENDENCIES GRAPH                               │
└─────────────────────────────────────────────────────────────────────────┘

                           ┌──────────┐
                           │  bot.py  │
                           └─────┬────┘
                                 │
                 ┌───────────────┼───────────────┐
                 │               │               │
         ┌───────▼────────┐  ┌───▼───┐  ┌──────▼──────┐
         │     config     │  │ database  │  handlers/  │
         │     (.env)     │  │    .py   │  __init__.py│
         └────┬───────────┘  └────┬─────┘  └──────┬────┘
              │                   │               │
              │         ┌─────────┼───────────────┤
              │         │         │               │
              │    ┌────▼──┐  ┌──▼──────┐  ┌────▼────────┐
              │    │orders │  │payments  │  │common,user, │
              │    │table  │  │table    │  │admin,ai,    │
              │    │       │  │         │  │payment      │
              │    └────┬──┘  └────┬────┘  │routers      │
              │         │         │        └────┬────────┘
              │         └─────────┼─────────────┤
              │                   │             │
         ┌────▼──────────────┐    │      ┌─────▼──────────┐
         │ LIQPAY_PUBLIC_KEY │    │      │handlers/       │
         │ LIQPAY_PRIVATE_KEY│    │      │payments.py     │
         │ LIQPAY_CALLBACK_URL     │      │  • proceed...  │
         └───────────────────┘    │      │  • select...   │
                                  │      │  • handle...   │
                        ┌─────────▼──────┴─ ┴──────────────┘
                        │
                   ┌────▼──────────────┐
                   │ payments/         │
                   │ liqpay_service.py │
                   │  • generate_url() │
                   │  • verify_sig()   │
                   │  • check_status() │
                   └────────┬──────────┘
                            │
                   ┌────────▼──────────┐
                   │ handlers/         │
                   │ webhook.py        │
                   │ • handle_liqpay_ │
                   │   webhook()       │
                   └───────┬───────────┘
                           │
                  ┌────────▼──────────┐
                  │ Port 8080         │
                  │ (aiohttp server)  │
                  │ /webhook/liqpay   │
                  └───────────────────┘

═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│                    SECURITY LAYER ARCHITECTURE                          │
└─────────────────────────────────────────────────────────────────────────┘

Incoming Request
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 1. HTTPS/TLS Layer                                  │
│    (Reverse Proxy - nginx)                          │
│    • Port 443 (HTTPS)                               │
│    • Certificate validation                         │
└────────────┬────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────┐
│ 2. Network Layer                                    │
│    • Firewall rules                                 │
│    • Port 8080 (internal only)                      │
│    • IP whitelisting (optional)                     │
└────────────┬────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────┐
│ 3. Application Layer                                │
│    • Signature verification                         │
│    • Base64 decoding                                │
│    • JSON validation                                │
└────────────┬────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────┐
│ 4. Cryptographic Verification                       │
│    • Private Key Storage (in .env)                  │
│    • SHA1(private + data + private) computation     │
│    • Constant-time comparison                       │
│      (prevents timing attacks)                      │
└────────────┬────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────┐
│ 5. Business Logic Layer                             │
│    • Order existence verification                   │
│    • User authorization check                       │
│    • Payment record validation                      │
│    • Database transaction integrity                 │
└────────────┬────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────┐
│ 6. Data Persistence                                 │
│    • Foreign key constraints                        │
│    • Unique constraints                             │
│    • Transaction atomicity                          │
│    • Audit trail (created_at, updated_at)          │
└─────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════

Architecture Version: 1.0
Status: ✅ Complete
Ready for Production: YES
